# Direct dependencies only; pip resolves the rest.
# Verified against Python 3.14 on Windows 11.

Flask==3.1.3          # API + serves the built React bundle
pywebview==6.2.1      # native window (uses the Edge WebView2 runtime)
pyinstaller==6.21.0   # bundles everything into the single .exe
